# 2026-07-31 변경내역
> 2026-07-31 작성 기록. 새 기록은 이 문서 마지막에 추가합니다.

## 목차

| 순서 | 시각(KST) | 작성자 | 변경 |
|---:|---:|---|---|
| 1 | 20:43 | Codex | [FE-B 에셋 및 홈 위젯 적용](#change-2026-07-31-01) |

---

<a id="change-2026-07-31-01"></a>

# Codex · 2026-07-31 · FE-B 에셋 및 홈 위젯 적용

**주요 파일**:
`public/assets/**`,
`src/frontend/components/travel-mvp-app.tsx`,
`src/frontend/components/common/direct-page-shell.tsx`,
`src/app/login/page.tsx`,
`src/shared/i18n.ts`

## 배경

전달받은 FE-B 압축본에는 현재 저장소보다 이전 버전의 애플리케이션 코드와 새 이미지 에셋이 함께 들어 있었다. 전체 코드를 덮어쓰면 이후 추가된 지도, 커뮤니티, 개인화 기능 등이 사라질 수 있으므로 현재 코드는 유지하고 새 에셋과 필요한 UI 연결만 선별 적용했다.

## 기존 ↔ 변경사항

| 구분 | 기존 | 변경사항 | 변경 이유 |
|---|---|---|---|
| 홈 대표 이미지 | 외부 Wikimedia 이미지 URL을 사용하고 실패 시 로그인 배경으로 대체 | 전달받은 `경주-야경-사진.jpg`를 로컬 정적 자산으로 사용 | 외부 네트워크 의존성을 제거하고 전달된 디자인 자산을 그대로 노출하기 위해 |
| 홈 카테고리 위젯 | Lucide 아이콘으로 관광지·맛집·체험·축제·지도·쇼츠·커뮤니티·전체보기를 표시 | 전달된 8개 카테고리 이미지를 사용하고 AI 추천은 `/courses`, 교통은 `/map`, 나머지는 기존 기능 경로에 연결 | 전달된 위젯 구성을 현재 앱의 실제 제공 기능과 일치시키기 위해 |
| 공용 탐색 아이콘 | 홈 검색과 하단 홈·프로필에 범용 벡터 아이콘 사용 | 전달된 검색, 선택 홈, 프로필 이미지를 해당 위치에 연결 | 화면 전반에서 FE-B 에셋의 시각 언어를 유지하기 위해 |
| 소셜 로그인 | 카카오 원격 이미지와 별도 Google 로고·텍스트 조합 사용 | 전달된 카카오·Google 전체 버튼 이미지를 로컬 경로에서 렌더링 | 원격 자산 의존성을 줄이고 피그마 산출물과 버튼 외형을 맞추기 위해 |
| 다국어 라벨 | AI 추천과 교통 홈 카테고리 라벨이 없음 | 한국어·영어·일본어·중국어 라벨 추가 | 언어 전환 시 새 위젯의 의미가 유지되도록 하기 위해 |
| 상호작용 피드백 | 일부 버튼의 눌림 크기가 `0.99`이거나 별도 피드백이 없음 | 변경한 버튼에 `0.96` 눌림 피드백과 최소 44px 수준의 터치 영역 적용 | 모바일에서 버튼 상태를 명확히 하고 터치 조작성을 유지하기 위해 |

## 상세 변경

- 압축본의 `public/assets` 파일 18개를 원본 이름과 디렉터리 구조를 유지해 추가했다.
- 용도가 확인되지 않은 `_inbox/용도미확인-u자-윤곽.png`는 목록 보존만 하고 코드에서는 참조하지 않았다.
- 홈 위젯 이미지에는 장식 이미지임을 나타내는 빈 대체 텍스트와 `aria-hidden`을 적용하고, 버튼 라벨로 기능 이름을 제공했다.
- 홈 전체보기 닫기 버튼과 로그인 버튼의 터치 영역 및 눌림 상태를 정리했다.

## 영향 및 호환성

- DB, ENV, API 계약, 인증 처리, 저장 데이터 형식은 변경하지 않았다.
- 카카오 로그인과 Google 안내의 클릭 로직은 유지하고 버튼 이미지만 로컬 자산으로 교체했다.
- 한글 파일명은 URL 인코딩을 통해 Next.js 정적 자산으로 정상 제공되는 것을 확인했다.

## 확인

- `npm.cmd run typecheck`: 통과.
- `npm.cmd test`: 테스트 파일 16개, 테스트 39개 모두 통과.
- 격리된 임시 작업본에서 `npm.cmd run build`: Next.js 프로덕션 빌드 및 46개 정적 페이지 생성 통과.
- 로컬 실행 서버 요청: `/`, `/login` 모두 HTTP 200.
- 정적 이미지 17개 요청: 모두 HTTP 200이며 응답 바이트와 원본 파일 크기가 일치.
- `git diff --check`: 통과.
- 시각 브라우저 검증: 인앱 브라우저가 연결되지 않아 실행하지 못함.
