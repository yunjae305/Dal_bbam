# 2026-09-06 변경내역

> 2026-09-06 작성 기록. 새 기록은 이 문서 마지막에 추가합니다.

## 목차

| 순서 | 시각(KST) | 작성자 | 변경 |
|---:|---:|---|---|
| 1 | 14:26 | Codex | [관광 요구사항 구현과 통합 검증](#change-2026-09-06-01) |
| 2 | 15:30 | Codex | [미리 제작한 문화재 쇼츠 등록·재생 흐름](#change-2026-09-06-02) |
| 3 | 23:30 | Codex | [공개 배포 준비 검사와 운영 인증·도구 보완](#change-2026-09-06-03) |

---

<a id="change-2026-09-06-01"></a>

# Codex · 2026-09-06 · 관광 요구사항 구현과 통합 검증

**주요 파일**: `src/app/api/`, `src/backend/`, `src/frontend/components/travel/`, `src/shared/`, `public/sw.js`, `supabase/migrations/20260906000*.sql`, `scripts/`, `tests/e2e/`, `.github/workflows/ci.yml`, `README.md`.

## 배경

사용자가 `요구사항명세서(관광).docx`의 전체 구현과 테스트를 요청했다. 원문의 기능 요구사항 33개와 비기능 요구사항 14개를 기존 구현에 대조한 뒤 누락된 사용자 흐름, 데이터 처리, 다국어 안내와 검증을 보완했다. 현행 아키텍처 결정에 따라 Next.js 16·React 19·Supabase를 유지했다. 항목별 근거와 외부 서비스 제약은 [요구사항 구현·검증 추적표](docs/requirements-implementation-2026-09-06.md)에 기록했다.

## 기존 → 변경 후

| 구분 | 기존 | 변경 후 | 변경 이유 |
|---|---|---|---|
| 홈·장소 분류 | 조회 이벤트 일부만 가져와 집계하고 제공자별 분류가 불일치 | 최근 7일 조회 전체를 SQL로 집계하고 1분 간격 갱신, TourAPI/Kakao 실제 분류에 따라 필터 적용 | 대량 조회의 랭킹 누락과 자연·체험 등의 잘못된 분류 방지 |
| 공개 데이터 응답 | 제공자 지연이 첫 화면을 오래 지연시킬 수 있음 | 최초 카탈로그 대기 최대 1.5초, 언어별 캐시와 동시 요청 병합, 장애 시 기존 데이터·명시된 샘플 제공 | 제공자 장애가 공개 화면 전체를 멈추지 않도록 하고 복구 후 실제 데이터로 돌아가기 위해 |
| 장소 상세 | 사용자 리뷰와 국가유산 보충 정보 연결 부족 | 장소별 리뷰·평점·작성 흐름, 국가유산 공식 API의 지역·정확한 명칭·식별키 검증 후 보충 정보 표시 | 명세의 상세 정보와 실제 방문자 평가를 한 화면에서 제공 |
| 쇼츠·AI | 피드 페이지 추가 로딩과 언어별 해설·음성 캐시 연결이 불완전 | 태그 유지 무한 로딩·재시도·공유 항목 탐색, 화면 내 재생, 생성 중복 방지, 언어별 해설·TTS 캐시와 음성 설정 | 긴 피드와 다국어 전환의 누락·중복 생성·재생 문제 해결 |
| 커뮤니티 | 지역·장소 분류 필터, 방문자 스토리와 저장 목록 연동 부족 | 지역·카테고리 검색, 맛집/숙소 별점 검증, 승인 사진 AI 초안·명시적 게시, 최근 공개 스토리·북마크 해제 반영 | 탐색→후기 작성→공유·저장 흐름을 연결 |
| 게시물 검사 | 텍스트/사진 검사 결과와 생성 결과의 처리 부족 | 다국어 개인정보·비속어 검사, 생성 전후 검증, 실제 EXIF 제거, 이미지 검사 실패·비AI 모드 상태 구분 | 민감정보 노출을 줄이고 검사 불가를 검사 성공으로 오인하지 않도록 처리 |
| 스탬프 | 기본 동기화 대상이 역사에 편중되고 기본 보상 번역 부족 | 실제 좌표의 역사·미식·자연 대상 선정, 기본 보상 칭호·설명 4개 언어 제공 | 명세의 테마별 수집 흐름과 언어 선택 반영 |
| 코스·일정 | 추천 결과와 날짜 일정의 연결, 이동시간·경로 연결 부족 | 일차별 도착·체류·이동시간, 제공자 경로/추정 구분, 시작일 선택으로 다일 일정 생성, 개별 방문 삭제·시간 편집 | 추천 결과를 실제 수정 가능한 여행 일정으로 이어지게 함 |
| 장바구니 | 일정·방문일 선택과 기존 방문의 시각·메모 보존 부족 | 원하는 일정/날짜에 추가하고 순서·시각·메모 보존, 중복 방지 | 다른 일정에 잘못 추가되거나 기존 일정이 손상되는 문제 방지 |
| 관리자 코스·DB 저장 | 관리자 편집 화면과 여러 단계 저장의 원자성 부족 | `/admin/courses` CRUD, 일정·큐레이션 메타데이터/장소 목록 트랜잭션, 서버 역할 전용 RPC | 중간 저장 실패 시 데이터 일부만 남는 문제와 타 사용자 수정 방지 |
| 동의·다국어 정책 | 가입 시 별도 필수 동의와 정책 전체 번역 부족 | 약관/개인정보 별도 필수 동의·버전·서버 시각 기록, 약관/개인정보/위치정보 정책 4개 언어 | 동의 여부를 서버에서 검증하고 선택 언어로 안내 제공 |
| 오프라인 | 기본 화면 자산·저장 장소 상세 탐색과 개인 캐시 경계 부족 | 오프라인 HTML/JS/CSS 준비, 공개 장소 설명·시간·연락처 조회, 개인 API 캐시 제외, 언어 버튼 대비 보완 | 온라인에서 준비된 캐시로 `/offline` 첫 방문을 지원하고 사용자별 데이터가 캐시로 재사용되지 않도록 함 |
| 프로덕션 이미지 | 한글 경로가 React 이미지 preload 응답 헤더에 그대로 들어가고 작은 관광지 카드도 고해상도 원본을 내려받음 | 로컬 이미지 URL 인코딩, 홈·로그인 배경/아이콘과 허용된 Wikimedia 관광지 이미지의 반응형 최적화 | 실제 브라우저의 `ERR_INVALID_CHAR` 실패와 Lighthouse에서 확인한 약 26MB의 불필요한 원본 전송을 줄임 |
| 검증 | 일부 단위 테스트와 단일 브라우저 검증 중심 | API·화면·실제 SQL 테스트, 4개 브라우저 설정, 격리 프로덕션 빌드·Lighthouse·100 동시 요청·읽기 전용 공급자 점검 | 사용자 흐름·데이터 무결성·장애 대응·성능을 서로 구분하여 재현 가능하게 검증 |
| 복구 DB 호환성 | 최신 마이그레이션 5개 누락, 일정·장바구니의 예전 `user_id` 필수 제약 유지 | 누락 스키마 진단, 필수 제약 보완을 포함한 6개 적용 SQL·읽기 전용 계획·트랜잭션 적용·실제 CRUD 검증 도구 준비 | DB 접속 성공과 실제 앱 저장 가능 여부를 구분하고 기존 데이터 손상 없이 복구하기 위해 |

## 상세 변경

- 원문 47개 요구사항 ID를 코드·테스트와 연결한 추적표와 실행 명령을 README에 추가했다.
- AI 사전 생성 스크립트의 `--dry-run`은 최소 50개 장소의 근거 데이터와 4개 언어의 예정 산출물 200개를 계산한다. 실제 해설/TTS 준비 상태를 확인하는 명령은 아니다. 복구 DB에서 `stamp_targets` 누락으로 사전 확인이 중단됐고 OpenAI 키도 미설정이므로 실제 200개 산출물 생성은 완료하지 못했다.
- 법적 안내의 기존 한국어 문구 71개를 보존하면서 영어·일본어·중국어를 추가했다. 실제 운영자·연락처·효력 발생일은 환경 설정으로 관리한다.
- 로컬 브라우저 바이너리·SQL 런타임·테스트 출력은 Git에서 제외한다. 테스트용 환경은 실제 개발 DB와 유료 AI를 사용하지 않는다.
- 사용자 요청으로 복구한 DB를 재점검했다. `scripts/migrate-database.mjs`와 `supabase/apply-pending-2026-09-06.sql`, [DB 작업 안내](docs/database-operations-2026-09-06.md)를 준비했다. 실제 CRUD는 `user_id NOT NULL` 충돌을 발견한 사전 검사에서 중단했으며 기존 사용자 데이터나 임시 행을 변경하지 않았다.

## 영향 및 호환성

- **DB**: `20260906000100_place_view_ranking.sql`, `20260906000200_course_schedule_workflows.sql` 추가. 실제 SQL 엔진에서 실행·재실행·권한·롤백을 검증했다. 사용자 복구 후 접속이 정상화됐지만 8월 말 세 개와 신규 두 개 마이그레이션이 누락되어 있다. 원격 SQL 실행용 연결은 아직 미설정이다.
- **ENV**: 선택 설정 `OPENAI_TTS_VOICE_KO/EN/JA/ZH`, `HERITAGE_API_ENABLED` 추가. 비밀키는 서버에서만 사용한다. `.env.local`은 변경하지 않았다.
- **DB 연결**: 마이그레이션 전용 `DATABASE_URL` 예시와 `db:plan`/`db:apply` 명령 추가. 현재 SQL 실행용 연결은 없으며 데이터 API 키와 별개다. `20260906000300_restored_actor_compatibility.sql`은 두 열의 null 허용만 보완한다.
- **API·데이터**: 관리자 코스·최근 스토리·국가유산 API 추가, 코스/일정 시간·경로와 필터·페이지 정보 확장. 회원가입 API는 현재 동의 버전과 두 필수 동의를 요구한다. 동의 시각은 서버가 기록하며 기존 OAuth 로그인은 유지한다.
- **인증·저장**: 기존 서버 세션과 `actor_key` 소유권 모델 유지. 새 저장 RPC는 `service_role`만 호출할 수 있다. 개인 API 응답을 Service Worker 공개 캐시에 넣지 않는다.
- **브라우저 검증**: 로컬 HTTP 프록시에서만 CSP의 HTTPS 강제 전환 지시문을 제거하고 WebKit 도우미에서만 로컬 세션 쿠키를 조정한다. 나머지 CSP와 실제 운영용 Secure 쿠키는 유지한다. 운영 HTTPS 검증과 구분한다.
- **운영 미검증**: 원격 저장·스토리지·소셜 로그인·실제 AI/TTS·사진 검사, 실제 단말 GPS, 배포 HTTPS/TLS, Android APK 설치는 외부 환경이 필요하며 로컬 자동 테스트 통과로 대체하지 않았다. 배포·커밋·푸시는 수행하지 않았다.

## 확인

- 타입검사 통과, ESLint 오류 0개·경고 28개.
- 단위·컴포넌트·API: 66개 파일, 345개 테스트 통과. 커버리지 Statements 59.68%, Branches 56.53%, Functions 65.92%, Lines 62.26%.
- PostgreSQL 18.3/PGlite 0.5.8: 실제 SQL 검증 23개 통과, 실패 0개. 새 마이그레이션 재적용·역할 권한·1,503건 집계·데이터 롤백 확인.
- DB 실행 도구 테스트 6개 통과. 읽기 전용 기본 모드·잘못된 연결 URL·원격 TLS 인증서 검사·롤백 실패 처리를 검증했다.
- 복구 DB 마이그레이션 SQL 검증 28개 추가 통과. 7월 스키마·예전 필수 제약을 재현해 6개 마이그레이션과 생성 SQL 묶음의 기존 행 보존·권한·저장·재적용·전체 롤백을 확인했다.
- 운영 의존성 `npm audit --omit=dev`: 알려진 취약점 0개.
- TourAPI·Kakao·국가유산 공식 API 읽기 성공. Supabase 복구 후 관광지 545건·좌표 545건·개요 503건 조회 확인. 필수 테이블 10개·열 11개·함수 10개가 누락되어 최신 기능 전체 연동은 미완료이며 OpenAI 키도 미설정이다.
- Lighthouse 모바일 LTE 시뮬레이션: 로그인 LCP 2.275초(성능 98/접근성 100), 홈 2.902초(95/96), 오프라인 2.337초(98/100). 세 경로 모두 3초 기준 통과. 초기 로그인 3.459초·홈 3.657초에서 개선했다.
- 공개 장소 API 부하: 100 동시 요청·총 1,000회·실패 0회·p95 84ms. 실제 원격 DB를 사용하지 않는 격리된 로컬 측정이다.
- Chromium 모바일/데스크톱·Firefox 데스크톱·WebKit 모바일에서 44개 E2E 통과, 실패 0개. 4개 언어·일정 편집/생성·장바구니 추가·동의 후 스탬프·개인 API 캐시 제외·API 응답 후 1초 내 상세 렌더링 확인.
- 오프라인은 준비된 Service Worker와 캐시를 전제로 새 화면 탐색·설명·운영시간을 검증했다. Chromium·Firefox는 브라우저 오프라인 설정, WebKit은 테스트 프록시의 앱 연결 차단을 사용했다. WebKit 자체 오프라인 에뮬레이션의 탐색 오류와 실제 단말 검증을 구분한다.
- 원격 SQL 접속 정보가 없어 마이그레이션 적용은 미완료다. 별도로 스탬프 공개/비공개 이미지 저장소 생성은 자동 승인 검토에서 공개 버킷에 대한 명시적 승인이 필요하다는 이유로 실행 전 거절됐다. 승인 요청 상태이며 저장소 생성·업로드·원격 SQL 변경은 수행하지 않았다.

---

<a id="change-2026-09-06-02"></a>

# Codex · 2026-09-06 · 미리 제작한 문화재 쇼츠 등록·재생 흐름

**주요 파일**: `src/app/admin/shorts/`, `src/app/api/admin/shorts/`, `src/backend/shorts-admin.ts`, `src/shared/admin-shorts.ts`, `src/shared/shorts-admin-messages.ts`, `src/shared/ui-messages.ts`, `src/frontend/components/travel/shorts-editor*`, `src/frontend/components/travel/shorts-screen*`, `scripts/test-environment.ts`, `tests/e2e/shorts-*.spec.ts`, `tests/fixtures/shorts-sample.mp4`, `docs/shorts-content-workflow.md`, `docs/requirements-implementation-2026-09-06.md`, `README.md`.

## 배경

사용자가 초기 쇼츠를 문화재가 인격화되어 소개하는 영상 또는 짧은 문화재 소개 영상으로 운영한다고 결정했다. 기존 MP4·YouTube 재생기와 관리자 저장 API를 활용해 준비된 영상을 등록·확인·공개하는 화면을 연결했다. 완성 영상의 원래 소리와 별도 AI 해설을 혼동하지 않도록 재생 흐름을 보완했다.

## 기존 → 변경 후

| 구분 | 기존 | 변경 후 | 변경 이유 |
|---|---|---|---|
| 관리자 운영 | 영상 저장 API만 있고 목록·편집 화면 없음 | `/admin/shorts`에서 장소·제목·소개·언어·길이·태그·MP4/YouTube 주소 입력, 미리보기, 초안 저장·수정·공개/비공개 전환 | 실제 제작한 영상을 운영자가 검수하고 공개하기 위해 |
| 관리자 조회·응답 | 공개 피드용 응답만 활용하고 편집 메타데이터 부족 | 인증된 GET 목록, 공개/초안·언어 필터, 제한된 페이지 조회와 편집용 원본 미디어·언어·공개 상태 반환 | 비공개 콘텐츠를 보호하면서 정확한 필드로 수정하기 위해 |
| 원고·미디어 검증 | 완성 영상에도 원고 필수 | 유효한 영상은 원고 생략 가능, 이미지 콘텐츠는 원고 유지, 공개 PATCH는 기존 행과 합친 미디어 검증·동시 변경 충돌 확인 | 불필요한 원고 입력을 없애고 재생 소스가 사라진 공개 항목 방지 |
| 영상 재생 안내 | MP4·YouTube에도 AI 음성 버튼 표시 | 별도 음성이 없는 완성 영상은 자체 소리로 재생하고 AI 버튼 생략, 문화재 상세 링크와 4개 언어 소개 문구 추가 | 완성 영상 재생이 AI 키나 생성 작업에 의존하지 않도록 함 |
| 기존 항목·페이지 | 새 영상 중심 검증으로 기존 이미지 편집이 막히고 게시 상태 변경 후 페이지 위치가 어긋남 | 이미지·원고 편집과 영상 제거 저장 지원, 기존 음성 보존, 저장 후 첫 페이지부터 목록 갱신 | 기존 콘텐츠 호환성과 다음 페이지 누락 문제 해결 |
| 작은 화면 | 강제 스크롤 스냅과 긴 카드로 Firefox에서 하단 버튼이 화면 밖에 고정됨 | 화면 높이에 맞춘 카드·중앙 정렬·완화된 스냅, 제목·소개 줄 수 제한, 가로 태그 목록, 불투명 상단 배경 | 실제 브라우저에서 재현된 버튼 접근 실패와 글자 겹침 해결 |
| 재생 검증 | 실제 완성 영상 흐름을 검증하는 브라우저 사례 부족 | 직접 만든 MP4로 시간·프레임 진행과 조작·저장·장소 이동·AI 요청 없음 확인, 오류 대체 화면·관리자 저장 실패 재시도 추가 | UI 상태 변경과 실제 영상 재생 성공을 구분해 확인하기 위해 |

## 상세 변경

- 새 화면은 한국어·영어·일본어·중국어를 지원하고 새 영상은 반드시 초안으로 저장한다. 서버 저장 성공 후에만 공개 상태를 바꾼다.
- 미리보기 버튼을 누를 때 MP4 또는 YouTube 콘텐츠를 불러온다. 제목·소개·태그·포스터·영상 주소와 길이를 검증한다.
- [운영 안내](docs/shorts-content-workflow.md)에 앱 내부 MP4, 외부 HTTPS MP4, YouTube 등록 방법과 선행 설정을 기록했다. 초기 운영 결정도 요구사항 추적표에 반영했다.
- 실제 문화재 영상 제작·업로드·게시를 수행하지 않았다. 브라우저 재생용 MP4는 외부 영상 없이 만든 패턴이며 서비스 콘텐츠에 포함하지 않는다.

## 영향 및 호환성

- **DB**: 새 마이그레이션·버킷 없음. 기존 누락 `shorts.video_url`·`youtube_video_id`와 관련 스키마 적용은 여전히 필요하다. 원격 SQL 연결이 없어 이번에도 실제 DB는 변경하지 않았다.
- **ENV·권한**: `.env.local`은 변경하지 않았다. 운영용 `ADMIN_EMAILS`가 비어 있어 관리자 이메일 설정이 필요하다. 브라우저 테스트 환경만 격리된 데모 계정을 관리자로 지정하고 실제 DB·AI 키를 비운다. 실제 관리자 인증·권한 검증을 우회하지 않는다.
- **API**: GET `/api/admin/shorts` 추가, POST/PATCH 응답을 편집 메타데이터로 확장. 기존 API의 공개 기본값은 유지하며 새 화면은 `isPublished:false`를 명시한다. 완성 영상의 `narration`은 빈 문자열 저장을 허용한다. 관리자 응답은 `private, no-store`다.
- **기존 콘텐츠**: 이미지·원고·별도 오디오 콘텐츠는 계속 재생·편집할 수 있다. 일반 피드는 공개 항목만 노출한다.

## 확인

- 단위·컴포넌트·API 68개 파일·385개 통과. 커버리지 Statements 60.63%, Branches 57.69%, Functions 66.29%, Lines 62.95%.
- 타입검사와 격리 프로덕션 빌드 통과. ESLint 오류 0개·경고 29개.
- 4개 브라우저 설정 전체 E2E 55개 통과·실패 0개·제외 1개. Chromium 모바일/데스크톱과 Firefox 데스크톱에서 MP4 실제 재생 시간 진행·일시정지·음소거·좋아요/저장·태그·장소 이동을 확인했다. 관리자 초안 저장·수정·공개·실패 후 재시도와 영상 오류 대체 화면은 WebKit을 포함한 네 설정에서 통과했다.
- Windows WebKit의 실제 디코딩이 `NotSupportedError`로 실패해 MP4 재생 검사 1개만 제외했다. 실제 Safari 단말 재생은 확인하지 않았다.
- 관리자 저장은 브라우저 fixture와 서버 API 계약 테스트로 검증했다. 원격 Supabase 저장·실제 YouTube 콘텐츠 재생·문화재 영상 등록을 검증한 결과는 아니다.
- 초기 32×32 MP4는 Firefox에서 시간 진행이 멈췄다. 320×180·1.338초·5,331바이트의 일반 MP4로 교체하고 앱과 독립된 재생기에서도 실제 프레임·시간 진행을 확인했다.
- 결과: `test-results/prerecorded-shorts-unit.log`, `prerecorded-shorts-e2e-final.log`, `shorts-final-fixture-decoding.json`. 관리자·피드의 모바일 및 데스크톱 화면 캡처를 확인했다. 이전 SQL·Lighthouse·부하 결과는 1번 기록의 검증 시점에 해당한다.
- 마지막 중앙 정렬·상단 배경 수정 후 쇼츠 E2E만 다시 실행해 7개 통과·실패 0개·동일 WebKit 디코더 제한 1개 제외를 확인했다. 최종 캡처에서 모바일과 Firefox 데스크톱의 카드 정렬·하단 버튼·상단 글자를 확인했다. 결과는 `test-results/prerecorded-shorts-visual.log`와 `test-results/e2e-shorts-visual/`에 남겼다.
- 브라우저 검사 종료 후 일반 `npm run build`도 통과해 `.next`를 테스트용 환경이 아닌 현재 로컬 운영 설정으로 다시 생성했다. `test-results/prerecorded-shorts-production-build.log`에 기록했다. `git diff --check` 통과. 커밋·푸시·배포는 수행하지 않았다.

---

<a id="change-2026-09-06-03"></a>

# Codex · 2026-09-06 · 공개 배포 준비 검사와 운영 인증·도구 보완

**주요 파일**: `src/shared/release-configuration*`, `src/backend/database-readiness*`, `src/backend/readiness*`, `src/backend/auth/`, `src/app/api/health/`, `src/app/api/auth/providers/`, `src/app/api/auth/google/login/`, `src/app/login/page.tsx`, `scripts/check-release*`, `scripts/provider-verification*`, `scripts/verify-providers.mjs`, `scripts/seed-curated-courses*`, `scripts/android-apk*`, `scripts/test-environment.ts`, `package.json`, `vercel.json`, `.github/workflows/ci.yml`, `.env.production.example`, `.vercelignore`, `README.md`, `docs/production-release.md`, `docs/requirements-implementation-2026-09-06.md`.

## 배경

사용자가 현재 앱을 배포해도 되는지 확인하고 배포 가능한 수준까지 구현하기를 요청했다. 실제 DB 접속 성공과 로컬 테스트 통과만으로는 운영 준비를 판정할 수 없어, 누락 스키마·저장소·콘텐츠·운영 설정을 배포 전에 검사하도록 보완했다. 현재 결과는 공개 배포 준비 미완료이며 외부 선행 조건을 임의의 값으로 채우지 않았다.

## 기존 → 변경 후

| 구분 | 기존 | 변경 후 | 변경 이유 |
|---|---|---|---|
| DB 준비 판정 | 단순 연결 성공으로 최신 기능의 저장 가능 여부를 충분히 구분하지 못함 | 런타임·CLI가 같은 읽기 전용 검사로 필수 테이블·열·RPC·nullable 조건·랭킹 실행·버킷 설정·콘텐츠 확인 | 복구 DB가 연결되지만 실제 일정·스탬프·쇼츠 운영은 불가능한 상태를 감지 |
| 배포 빌드 | 일반 빌드가 운영 설정 누락을 막지 않음 | `release:check`·`build:release` 추가, Vercel 빌드에 선행 검사 연결 | 로컬 주소·예시 비밀키·운영자 미설정·미완료 DB가 공개 배포로 넘어가는 것을 차단 |
| 운영 데모 | 데모 설정과 관리자 이메일 조합이 운영 인증에 영향을 줄 수 있음 | 일반 프로덕션 데모 로그인 차단, 외부 키가 없는 loopback 격리 테스트만 명시적으로 허용 | 공용 테스트 자격증명을 실제 사용자·관리자 인증으로 재사용하지 않도록 함 |
| Google 로그인 | 제공자 비활성 상태에서도 로그인 진입 가능 | Supabase 인증 설정을 조회·캐시하고 로그인 화면과 API에서 비활성 제공자 안내 | 실제 확인된 Google 비활성 상태에서 실패하는 OAuth 진입 방지 |
| 큐레이션 시드 | 기존 항목 삭제 후 재생성하여 ID·공유 링크가 바뀔 수 있음 | 모든 장소·중복·필수 RPC를 쓰기 전에 확인, 코스별 원자적 RPC로 기존 ID·토큰 보존 | 사전 오류로 인한 부분 변경과 기존 공유 링크 손상 방지 |
| Android 도구 | Windows 명령 실행·비밀번호 인자와 테스트용 도메인 연결 파일에 문제 가능 | 배치 경로·인자 처리 검증, keytool 비밀번호를 환경변수로 전달, 디버그 연결 파일을 별도 산출물에 저장 | 실행 오류·비밀값 노출·운영 assetlinks 덮어쓰기 방지 |
| 설정·검증 안내 | 코드 검증과 실제 출시 조건이 여러 문서에 흩어짐 | 비밀값 없는 운영 예시, Git·배포 제외 비공개 설정 초안, 배포 순서·최신 판정·실기기 미검증 항목 정리 | 운영 계정·콘텐츠·SQL 연결을 준비한 뒤 같은 검사로 후속 작업을 이어가기 위해 |

## 상세 변경

- 공개 HTTPS 오리진, 콜백·지도 도메인 일치, 강한 서버 비밀키, 실제 관리자·운영자 정보, 명시적 AI 사용 여부를 검사한다. 검사 오류에는 원본 환경변수 값을 넣지 않는다.
- DB 검사는 총 8초 제한이며 상태 결과를 30초 캐시하고 동시 요청을 병합한다. RPC 존재 여부는 스키마 메타데이터를 읽으며, 실제 호출은 읽기 전용 랭킹으로 제한한다. 영상은 최대 100개 공개 행의 소스 형식·연결 장소를 확인하며 파일 존재나 실제 재생 성공을 보장하지 않는다.
- 운영 설정 초안은 기존 제공자 설정을 복사하고 CRON·관리자 API 비밀키를 새로 생성해 `artifacts/release/.env.production.local`에만 보관했다. 자동 로드·업로드하지 않으며 도메인·운영자·관리자·AI 모드는 아직 빈 값이다. `.env.local`은 변경하지 않았다.
- 읽기 전용 실제 검사에서 DB 연결과 Kakao 장소 API 응답을 확인했다. DB 구조·저장소·콘텐츠는 미완료이고 TourAPI는 이번 검사 제한 시간 안에 응답하지 않았다. Supabase 인증 설정은 Google 비활성으로 확인됐다.

## 영향 및 호환성

- **DB·저장 데이터**: 새 마이그레이션을 추가하지 않았고 원격 DB·시드·버킷을 변경하지 않았다. 기존에 준비한 6개 마이그레이션 적용과 콘텐츠 등록이 필요하다. 시드는 코스 하나의 저장을 원자적으로 처리하며 전체 배치가 하나의 트랜잭션인 것은 아니다.
- **ENV·배포**: `DEMO_ISOLATED_TEST`를 추가했다. 공개 운영은 `DEMO_MODE_ENABLED=false`, `DEMO_ISOLATED_TEST=false`여야 한다. `FEATURE_AI`는 명시해야 하며 활성화하면 OpenAI API 설정이 필요하다. 일반 `build` 성공은 `build:release` 준비 검사 통과를 의미하지 않는다.
- **API·인증**: GET `/api/auth/providers`는 Google 사용 가능 여부만 반환한다. `/api/health`는 운영 준비 미완료 시 정상으로 판정하지 않으며, `kakaoMobility`를 검사하지 않은 `null`로 표시하고 실제 사용자 흐름의 미검증 상태를 별도 제공한다. 공개 응답에는 비밀키·사용자 행·환경변수 원문을 포함하지 않는다.
- **Android**: 기존 공개 `assetlinks.json`을 보존했다. 현재 파일도 기존 테스트 인증서에 연결되어 있어 정식 출시용 교체가 필요하며, APK 빌드·설치·Play 업로드를 수행한 것은 아니다.
- **외부 조건**: SQL 실행용 연결·Vercel 프로젝트 연결·제어 가능한 로그인 브라우저가 없고 관리자/운영자 정보·영상·AI 설정이 미완료다. 스탬프 공개 버킷 생성은 앞선 자동 승인 검토에서 명시적 공개 저장소 승인이 필요하다는 이유로 실행 전에 거절된 상태이며 재시도하지 않았다.

## 확인

- `test:coverage`: 77개 파일·478개 통과. Statements 66.12%, Branches 63.29%, Functions 71.46%, Lines 68.35%.
- `test:release-tools`: Node 테스트 20개 통과·실패 0개. 배포 검사·공급자 검사·시드·Android 도구를 포함한다.
- 타입검사 통과. ESLint 오류 0개·경고 29개.
- 격리 빌드 후 4개 브라우저 설정 E2E 55개 통과·실패 0개·제외 1개. Windows WebKit MP4 디코더 제한으로 실제 재생 한 사례만 제외했다.
- E2E 종료 후 일반 `npm run build`도 통과하여 `.next`를 현재 로컬 설정으로 재생성했다. 배포 준비 검사는 **`ready:false`**다. 최신 JSON 기준 DB reachable=true, schema/storage/content=false, TourAPI=false, Kakao=true이며 배포된 URL 검사는 수행하지 않았다.
- 실행 근거: `test-results/release-unit.log`, `release-tools.log`, `release-lint.log`, `release-typecheck.log`, `release-build-test.log`, `release-e2e.log`, `release-production-build.log`, `release-readiness-current.json`. 이전 시각의 같은 이름 `.log` 공급자 결과와 최신 `.json`을 혼합하지 않았다.
- SQL 51개·DB 도구 6개, Lighthouse·100 동시 요청·의존성 검사는 앞선 1번 작업의 결과이며 이번 보완 후 재실행하지 않았다. 실제 HTTPS OAuth·메일 수신·현장 GPS·운영 DB 저장·AI 생성·문화재 영상 재생은 미검증이다.
- `git diff --check` 통과. 커밋·푸시·실제 배포는 수행하지 않았다. 현재 판정과 후속 순서는 [운영 배포 안내](docs/production-release.md)에 기록했다.
